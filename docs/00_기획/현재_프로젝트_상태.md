# SCHAT 현재 상태

> 이 문서는 과거 이력이 아니라 현재 실제 코드와 검증 상태만 설명한다.

## 기준 상태

| 항목 | 현재 값 |
|---|---|
| 공식 안정 복구 기준점 | **v0.9** |
| 작업 브랜치 | `boha-rag` |
| v1.0 준비 상태 | **최종 READY 재판정 전** |
| 최신 문서 현행화 기준 | 2026-09-21 |
| 외부 평가 호출 | 기존 Groq 기록 유지, Gemini Embedding 2 요청 168회, Gemini controlled-generation 3 case 평가 |
| 외부 전송 범위 | 승인된 evaluation-only 범위만 사용, 원문·질문·응답은 artifact에 저장하지 않음 |
| Git commit/tag/push | 0회 |

`v0.9`와 이전 `v0.8` tag는 이동하거나 변경하지 않았다.

## Production 구조

```text
질문 분석
  → 명시적 문서 범위 + 질문 의미 결합
  → BM25 + MiniLM semantic/vector
  → RRF + existing reranker
  → context expansion / parent atomicity
  → pre/post evidence gate
  → Facet-slot SourceUnit selection
  → server reconstruction
  → AnswerCoverage + validate_answer()
  → presentation sidecar
  → Streamlit 답변·출처 UI
```

- Production retrieval/embedding/validator 변경: 0건
- 문서 등록 단계에는 청크 문장을 바꾸지 않는 형식·품질 자동검사가 추가됨
- Facet-slot distinct SourceUnit limit: 16
- Q006와 운영 out-of-scope: provider transport 0회
- Citation, number, unit, time, condition, negation, action, branch, phase와 source-order validator 유지

## Gold·Retrieval·RAGAS

- Approved Positive Gold: **21건**
- Deferred: **11건**, 자동 승인 0건
- Approved abstention Gold: **4건**
- Production hybrid: Hit@10 0.9524, MRR 0.7370, ID Precision 0.5365, ID Recall 0.8210
- BM25 current: Hit@10 0.9048, MRR 0.5822
- ChromaDB + MiniLM: Hit@10 0.3333, MRR 0.1093
- ChromaDB + Gemini Embedding 2: Hit@10 0.9524, MRR 0.4337, ID Precision 0.3095, ID Recall 0.5593
- Gemini Embedding 2 ChromaDB는 147개 문서 청크와 21개 질문을 사용한 evaluation-only이며 Production에는 연결하지 않았다.
- E5와 BM25+E5: evaluation-only
- 과거 Groq Live RAGAS: `STOPPED_ON_CRITICAL_FACT_PRESERVATION`
- Gemini controlled-generation 재평가: `UAT-T02` 저장 로직 수정 완료, `UAT-T12` evaluation 검사기 구간 결합 오탐 수정 완료, `UAT-T11` required coverage 보강 완료·Live 재평가 대기
- Faithfulness/Answer Relevancy: 원 질문·generated answer의 judge 전송 권한이 없어 pending

## 기능 상태

| 영역 | 상태 |
|---|---|
| Text retrieval / verified extractive answer | Production 운영 |
| Production retrieval | BM25 + MiniLM + RRF + existing reranker 유지 |
| Presentation sidecar | Production 운영 |
| Controlled generation | Evaluation-only Gemini 3 case 재평가, T12 검사기 오탐 수정 완료, T11 required coverage 보강 완료·Live 재평가 대기 |
| Structured table | Approved 5/5, 운영 승인 subset 2/2 Top-10 |
| Image/diagram | 10/10 사람 검수 완료, Production Gold 미승인·v1.0 범위 제외 |
| Operational UAT | 마지막 전체 운영 검증 **36/36 PASS** |
| Full pytest | 최신 실행 **985 passed, 5 skipped, 15 warnings** |
| 문서 전용 테스트 | **9 passed** |
| ChromaDB | MiniLM·Gemini Embedding 2 비교 완료, Production 미통합 |
| Qdrant | Production 미통합, 향후 후보 |

## 사람이 해야 할 다음 작업

1. 강화된 coverage 계약으로 `UAT-T11`을 Live 재평가한다(외부 호출은 별도 승인 후 실행).
2. T12의 과거 candidate에 대해 사람이 판정한 별도 `검사 시행 누락`은 다음 재평가 때 다시 확인한다.
3. 검색 결과와 Gold의 독립적인 사람 검수 범위를 확정한다.
4. 위 작업 이후 v1.0 readiness와 Live RAGAS 범위를 다시 판정한다.
