# 테스트결과서

기준일: 2026-09-17 · 상태: 문서 정비 점검 완료, 기능·성능 비교 미평가

## 1. 결과를 읽는 기준

이번 실행은 코드·문서·Git 이력 점검이다. 병원 지침으로 질문을 실행하거나 LLM·RAGAS를 호출하지 않았다. 테스트 코드 존재, 타 브랜치 과거 성적, 현재 실제 실행 결과를 구별한다. 이번 문서의 미평가 값은 0점이 아니다.

## 2. 테스트 환경

| 항목 | 현재 확인 |
|---|---|
| 점검 날짜 | 2026-09-17 |
| 환경 | Windows, PowerShell, 프로젝트 `.venv` Python 3.12.10 |
| 코드 | main `8c44851` + 작업 시작 전부터 있던 UI 미커밋 변경 |
| 데이터셋 | 비교 데이터셋 미확정·미실행. 실제 원본·운영 DB 미조회 |
| 실제 질문 수 | 이번 작업 0개 |
| LLM | 호출 안 함. 실제 설정 모델 확인 안 함 |
| Prompt Version | 실행 없음. 코드 AI_VERSION=8, SYSTEM 해시의 평가 기록은 미수집 |
| Top-k | 실행 없음. 현재 코드 후보/seed/문맥 값은 RAG데이터설계서 참조 |
| pytest / ruff | `importlib.util.find_spec` 확인 결과 둘 다 없음. 이번 작업 미설치·미실행 |
| RAGAS | 현재 코드·의존성에서 미확인, 도입 예정 |
| ChromaDB | 현재 코드·의존성에서 미확인. 담당 브랜치 확인 필요 |

## 3. 이번에 수행한 점검

| 점검 | 방법 | 결과와 한계 |
|---|---|---|
| 브랜치·기준선 | git status, branch, rev-parse | main 기준 확인, 기존 미커밋 소스 3개 확인 |
| 기존 문서 | 파일 목록, git ls-tree/log/show | 현재 docs 없음, 다른 브랜치 기존 BM25/RAG 문서 확인·보존 |
| 실제 Retrieval | 코드·호출 경로·의존성 대조 | BM25 + FAISS/pgvector Hybrid 확인 |
| 테스트 코드 규모 | Python AST로 test 파일·함수 수 집계 | 9개 test 파일, 135개 test_ 함수. 매개변수화된 실제 테스트 건수와 다름 |
| 테스트 도구 | Python 모듈 탐색 | pytest/ruff 없음. 회귀 테스트 통과 여부 미판정 |
| 표준 문서 | 8종 + README 존재·본문·로컬 링크 점검 | 문서 검수 수행. 기능 실행 성공을 의미하지 않음 |
| 소스 보존 | 작업 전후 소스·테스트 SHA-256와 Git 상태 대조 | 이번 작업 소스 변경 없음 |

## 4. BM25·ChromaDB 공통 비교 결과

평가 실행 ID: 미발급. 담당자·데이터셋·질문 수·동일 조건 승인: 확인 필요.

| 평가 지표 | BM25 | ChromaDB | 비고 |
|---|---:|---:|---|
| Faithfulness | 미평가 | 미평가 | RAGAS 도입 예정 |
| Answer Relevancy | 미평가 | 미평가 | 같은 질문·LLM·평가기 사용 필요 |
| Context Precision | 미평가 | 미평가 | 순위·k·문맥 처리 조건 고정 필요 |
| Context Recall | 미평가 | 미평가 | 사람 검수 정답표 필요 |
| 평균 | 미평가 | 미평가 | 산출 정의 미확정, 임의 계산 없음 |

현재 Hybrid의 결과를 이 표의 순수 BM25 결과로 넣지 않는다. 이후 Hybrid를 별도 기준선으로 평가하면 동일 문서에 실행 방식과 조건을 명시한 절을 추가한다.

## 5. 정성적 평가

| 관점 | 이번 실제 결과 | 다음 확인 |
|---|---|---|
| 검색 성공 사례 | 미평가 | 기대 문서·쪽수·필수 인용과 검색 결과 대조 |
| 검색 실패 사례 | 미평가 | 추출·분할·질의 확장·권한·순위 단계 구분 |
| 근거 누락 | 미평가 | 여러 chunk가 필요한 절차의 누락 확인 |
| 잘못된 답변 | 미평가 | 조건·부정·수치·단위·비교 대상 보존 확인 |
| 답변 품질 차이 | 미평가 | 동일 조건의 두 버전 실제 답변 확보 |
| 개선 필요사항 | 코드상 확인 항목만 존재 | 운영 문서 교체 부분 실패, 타 브랜치 계약 일치, 최신 UI 회귀 확인 |

새 사례는 `실행 ID / 질문 ID / Retrieval 방식 / 기대 근거 / 실제 결과 / 성공·실패·판단보류 / 원인 / 검수자`로 기록한다. 정본에는 승인되지 않은 병원 원문이나 개인정보를 붙이지 않는다.

## 6. 다른 브랜치의 과거 결과

`12b48f8:docs/CHANGELOG.md`에는 2026-09-12 BM25 일괄 평가 구현의 테스트 결과가 기록돼 있다. `dbad812:docs/bm25/`와 `docs/rag/`에도 별도 실험 결과가 존재한다. 이번에 그 실행을 재현하지 않았으므로 숫자를 현재 결과표로 가져오지 않았다. 기존 문서·코드·실행 조건은 [문서 관리 README](../README.md)의 Git 참조를 통해 별도로 검토한다.

## 7. 결론

표준 산출물 작성에 필요한 구조·기능 근거는 확인했다. 두 버전의 품질 우열, 운영 적합성, 회귀 테스트 통과 여부는 이번 결과로 판정할 수 없다. 다음 단계는 담당자가 비교 기준선·정답표·실행 환경을 확정하는 것이다.
