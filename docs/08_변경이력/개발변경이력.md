# 개발변경이력

이 문서는 날짜별 History다. 현재 기준 정본은 [문서 목록](../README.md)에서 확인한다. Git으로 확인되지 않은 과거 날짜·작업은 새로 만들어 넣지 않는다.

## 2026-09-17

### 작업 버전

- 공통 — BM25/ChromaDB 비교를 위한 표준 산출물 정비.

### 작업 내용

- main `8c44851`과 기존 미커밋 UI, 검색·저장·답변·평가 코드 분석.
- 현재 앱의 BM25 + FAISS/pgvector Hybrid 확인. ChromaDB·RAGAS는 현재 소스·의존성에서 미확인.
- 현재 작업 폴더에 docs가 없음을 확인하고 공통 표준 문서 8종과 README 생성.
- `origin/codex/two-stage-rag-plan`과 `origin/boha-rag`에 존재하는 기존 문서는 변경하지 않고 Git 참조를 기록.

### 변경된 기능

- 없음. 소스·UI·검색·LLM·설정·의존성 변경 없음.
- 작업 전부터 존재하던 `mvp/app.py`, `mvp/ui.css`, `mvp/ui.py` 변경은 그대로 유지. 작성 날짜를 추측해 과거 개발 이력으로 만들지 않음.

### 관련 문서

- [관리 README](../README.md)
- [요구사항](../01_요구사항/요구사항명세서.md), [화면](../02_화면기능설계/화면기능설계서.md)
- [시스템](../03_시스템설계/시스템아키텍처설계서.md), [RAG·데이터](../04_RAG데이터설계/RAG데이터설계서.md)
- [테스트계획](../05_테스트/테스트계획서.md), [테스트결과](../06_테스트결과/테스트결과서.md)
- [사용자매뉴얼](../07_사용자매뉴얼/사용자매뉴얼.md), 본 개발변경이력

### 테스트 결과

- 소스·Git·의존성 정적 조사. 테스트 함수 135개/파일 9개 확인(실행 건수 아님).
- pytest/ruff 모듈 없음. 기능 회귀·실데이터·LLM·RAGAS 평가 미실행.
- 문서 존재·링크·본문·소스 보존 점검. 구체적 범위는 테스트결과서 참조.

### 다음 작업

- 담당자가 공통 정본과 기존 브랜치 문서의 용어·범위를 검토.
- 순수 BM25 비교 경로, ChromaDB 브랜치, 동일 데이터셋·정답표·Prompt 조건 확정.
- 최신 UI 변경의 코드 기준선 확정과 테스트 환경 준비.
- RAGAS 도입·실행은 별도 작업으로 진행. 이번에는 설치하지 않음.

## 2026-09-14

### 작업 버전

- 다른 브랜치에서 관리: `origin/boha-rag`의 BM25·RAG 관련 기록.

### 작업 내용

- Git 커밋 `dbad812`의 날짜와 제목 `RAG source unit selection progress`, `docs/bm25/`·`docs/rag/` 문서 경로 존재 확인.

### 변경된 기능

- 이 문서에서는 현재 main의 추가 구현으로 판정하지 않음. 타 브랜치의 실제 기능·병합 범위는 담당자 확인 필요.

### 관련 문서

- `dbad812:docs/rag/01_ARCHITECTURE.md` 및 같은 커밋의 `docs/bm25/`, `docs/rag/`.

### 테스트 결과

- 기존 브랜치 기록 참조. 이번에 재실행·성적 전재하지 않음.

### 다음 작업

- 기존 실험 이력을 유지하고 공통 정본에 통합할 차이를 담당자가 검토.

## 2026-09-12

### 작업 버전

- 공통 앱 및 BM25 관련 기록. 브랜치별로 구분해 보존.

### 작업 내용

- main에 포함된 `fda9c7d`: 관리자 BM25 검색 디버그·내보내기 추가. `8c44851`에서 관련 PR 병합.
- main에 포함된 `bd18d6b`: PDF 줄바꿈 문장 검증과 제목-only 검색 seed 제외.
- main에 포함된 `2de3c40`: 목적 근거 보존과 관리자 검색 진단.
- 다른 브랜치 `12b48f8`: BM25 일괄 기준선 평가 및 기존 docs 추가. 현재 main 기능으로 혼합하지 않음.

### 변경된 기능

- main: 관리자 BM25 CSV·진단 및 근거 검증 개선 코드 확인.
- 다른 브랜치의 일괄 평가: 그 브랜치 문서에서 관리. 현재 main에는 해당 모듈이 없음.

### 관련 문서

- 루트 README와 `mvp/diagnostic_ui.py`, `mvp/diagnostics.py`, `tests/test_wrapped_evidence.py`.
- `12b48f8:docs/00_PROJECT_GOAL.md`, `docs/01_BM25_PLAN.md`, `docs/02_BM25_RESULT.md`, `docs/CHANGELOG.md`.

### 테스트 결과

- 과거 실행 로그를 이번 작업에서 재현하지 않음. 타 브랜치 기록은 해당 커밋의 결과 문서를 참조하며 현재 결과와 분리.

### 다음 작업

- 당시 이후의 기능 검증 완료 여부는 해당 브랜치 기록과 담당자에게 확인. 현재 정본의 미평가 항목은 별도 실행으로 갱신.
