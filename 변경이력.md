# SCHAT 변경 이력

> 사람이 주요 변화를 빠르게 확인하는 문서다. 상세 구현 과정은 `docs/rag/`, `docs/bm25/`, `docs/superpowers/progress/`에 보존한다. `v0.8` 이전 milestone에는 임의 버전 번호나 소급 tag를 붙이지 않았다.

| 버전 | 날짜 | 핵심 변경 | 변경하지 않은 것 | 테스트 | 복구 기준점 |
|---|---|---|---|---|---|
| Git tag 없음 · BM25 기준선 | 2026-09-12 | 진정간호 BM25 Top-K와 relevance 평가, title-only 문제 확인 | RAG generation | 당시 결과 문서와 review HTML | commit `8c44851` 부근, 별도 tag 없음 |
| Git tag 없음 · RAG SourceUnit | 2026-09-13~14 | Evidence gate, SourceUnit, coverage, Facet-slot selection과 Q002 Live 완주 | BM25/embedding 핵심 계약과 validators | 단계별 Mock/Live 결과 | commit `dbad812` 이후 작업, 별도 tag 없음 |
| Git tag 없음 · 질문 일반화/표시 | 2026-09-15~16 | Parent atomicity, query generalization, UAT, presentation sidecar | Retrieval 전략과 exact citation | 진정 UAT 45/45, Q006 zero-call | commits `156cc90`, `7214705`; tag 없음 |
| Git tag 없음 · 수혈 등록/평가 | 2026-09-16~17 | 수혈 105 chunks, retrieval 90문항, BM25/Chroma/MiniLM 공정 비교 | Production retrieval과 generation | IR/RAGAS ID metric, full regression | HEAD `9000d98`; 후속 결과는 작업 트리 미커밋 |
| **v0.8** | 2026-09-18 | E5-large와 BM25+E5 비교, text UX/controlled generation Mock, 표 evidence/UI, multimodal routing 준비 | Production BM25+MiniLM+RRF+reranker, validators, provider | **525 passed, 4 skipped**, Ruff/diff/security/code review 통과 | annotated tag `v0.8` |

## 현재 버전

### v0.8

현재 안정 상태를 실제 commit과 annotated tag로 저장한 첫 복구 버전이다.

### 핵심 변경

- 진정간호 42 chunks와 수혈간호 105 chunks를 같은 catalog 구조로 관리한다.
- Production은 `BM25 + MiniLM semantic/vector + RRF + reranker`를 유지한다.
- 수혈 90문항의 동일 조건에서 BM25, MiniLM, E5-large, RRF, reranker와 selective fallback을 비교했다.
- Raw retrieval 최상위는 `BM25 + E5 RRF`였지만 기존 reranker·latency·memory까지 고려해 production을 바꾸지 않았다.
- Facet-slot SourceUnit 선택, server reconstruction, AnswerCoverage와 citation validator를 유지한다.
- Presentation sidecar를 통해 검증된 답변을 질문 유형별로 읽기 쉽게 표시한다.
- Controlled generation은 provider-neutral Mock validator까지만 준비했다.
- Table 5개/33행의 evaluation-only 검색·인용·로컬 UI를 완료했다.
- Image/TF027과 provider Live는 승인 전까지 pending이다.

### 변경하지 않음

- Production retrieval와 embedding
- Facet-slot, selection limit 16와 SourceUnit identity
- Parent atomicity, PromptCoverage, AnswerCoverage
- Citation, number, unit, time, condition, negation, action, source-order validator
- Q006 zero-call
- Production dependency와 외부 provider 정책

### 테스트

- Full pytest: `525 passed, 4 skipped, 7 warnings`
- Integrated focused regression: `236 passed, 4 warnings`
- Q006 zero-call: `1 passed`
- Ruff: 통과
- `git diff --check`: 통과
- Artifact security audit: exact source match 0, forbidden exact key 0
- Code review: 추가 조치가 필요한 결함 없음

### 복구

- Branch: `boha-rag`
- Tag 상태: annotated tag `v0.8`
- 안정 commit 확인: `git rev-parse v0.8^{commit}`
- 복구 방법: [`복구기준점.md`](복구기준점.md)

## 앞으로의 버전 제안

과거 commit에는 소급 tag를 만들지 않는다. 사용자 승인 후 안정 milestone마다 annotated tag 하나를 만드는 방식을 권한다.

| 제안 | 기준 |
|---|---|
| `v0.8` | 현재 text/table MVP 안정 상태 — 저장 완료 |
| `v0.9` | Provider generation과 image/multimodal의 승인된 검증 완료 |
| `v1.0` | 실제 운영 UAT와 배포 승인까지 완료한 실사용 MVP |

## 문서 역할

- `docs/현재정본/`: 현재 최신 상태
- `docs/superpowers/progress/`: 작업 진행 히스토리
- `docs/rag/`: RAG 상세 기술 기록
- `docs/bm25/`: BM25 상세 기술 기록
- `artifacts/`: 평가 결과와 증거
- `변경이력.md`: 사람이 보는 주요 변경 기록
- `복구기준점.md`: Git 안정 지점과 복구 안내
