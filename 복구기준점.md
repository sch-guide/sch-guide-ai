# SCHAT 복구 기준점

## 현재 안정 상태

| 항목 | 값 |
|---|---|
| Branch | `boha-rag` |
| 안정 commit | annotated tag `v0.8`이 가리키는 commit |
| HEAD와 upstream | 저장 완료 시 `origin/boha-rag`와 동일 |
| 저장일 | 2026-09-18 |
| Production retrieval | BM25 + MiniLM semantic/vector + RRF + reranker |
| Production embedding | `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`, 384d |
| Full pytest | 525 passed, 4 skipped, 7 warnings |
| Ruff | 통과 |
| `git diff --check` | 통과 |
| Security audit | 통합 artifact forbidden exact key 0; 복구 후보 artifact text 129개에서 exact source/secret value 0 |
| Actual tag | `v0.8` |

## 중요한 현재 상태

2026-09-17까지 검증한 최신 기능·평가·문서는 선택된 범위만 하나의 안정 commit과 `v0.8` tag로 저장했다. 따라서:

- `v0.8`은 production 코드, tests, 평가 도구, 현재정본과 raw-free 근거 artifact를 함께 가리킨다.
- `tmp/`, model/venv cache, 병원 원본과 superseded artifact는 의도적으로 포함하지 않았다.
- 정확한 commit은 `git rev-parse v0.8^{commit}`으로 확인한다.

## 왜 안정 상태 후보인가

- Full pytest가 통과했다.
- Ruff와 whitespace diff 검사가 통과했다.
- Sedation UAT 45/45와 Q006 zero-call 계약을 유지했다.
- 수혈 retrieval metric을 독립적으로 재계산했다.
- Artifact에서 병원 exact source와 금지 key 노출이 없었다.
- Code review에서 조치가 필요한 correctness, safety, regression 결함이 없었다.
- Production retrieval/embedding/dependency를 최신 evaluation 때문에 바꾸지 않았다.

따라서 현재 상태는 **저장된 안정 복구 기준점 `v0.8`**이다.

## v0.8 검증 체크

| 조건 | 결과 |
|---|---|
| Full pytest PASS | 예 |
| Ruff PASS | 예 |
| `git diff --check` PASS | 예 |
| Security audit PASS | 예 |
| 주요 regression PASS | 예 |
| Code review PASS | 예 |
| 선택적 staging 검수 | 168개 대상, 제외 경로 0개 포함 |

## 저장에서 제외할 경로

다음은 임시 또는 로컬 cache이므로 안정 commit에 넣지 않는다.

- `tmp/`
- `.tmp/`
- `.pytest-tmp-*`
- `.pytest_tmp/`
- `.pytest_cache/`
- `.ruff_cache/`
- `.venv/`, `.venv-eval-transfusion/`
- `data/models/`
- API key, `.env`, secrets, 병원 원본 PDF
- `artifacts/2026-09-16_transfusion-chromadb-ragas-baseline/chroma_index/` — 재생성 가능한 HNSW binary cache
- `artifacts/2026-09-16_schat-mvp-stabilization/` — 2026-09-17 최종본 이전 결과
- `artifacts/2026-09-16_transfusion-expanded-retrieval/` — 2026-09-17 최종본 이전 결과
- `artifacts/2026-09-17_bm25-e5-retrieval-strategy/` — `-02` 최종 검수본 이전 결과

`data/library/catalog.sqlite3`는 이미 현재 HEAD에 포함되어 있고 이번 작업에서 새로 stage할 필요가 없다.

## 복구 기준점 저장 명령

아래는 `v0.8` 저장에 사용한 선택 범위와 검증 절차다. `git add .`을 사용하지 않았다.

### 1. 상태 재확인

```powershell
git status --short
git branch --show-current
git rev-parse HEAD
git rev-parse '@{upstream}'
git diff --check
```

Branch가 `boha-rag`이고 저장 전 HEAD와 upstream이 같음을 확인했다.

### 2. 실제 내용이 바뀐 Production·설정을 명시적으로 stage

```powershell
git add -- AGENTS.md mvp/ai.py mvp/answer_ui.py mvp/presentation.py mvp/query.py mvp/controlled_generation.py mvp/evidence_routing.py
```

`mvp/.env.example`, `mvp/admin_ui.py`, `mvp/app.py`, `mvp/library.py`, `mvp/retrieval.py`, `mvp/settings.py`는 현재 `git status`에는 수정으로 보이지만 `git diff --name-only`에는 나오지 않는 line-ending/stat 상태다. 실제 내용 변경이 아니므로 복구 commit의 변경 파일로 세지 않는다. Commit 직전에도 같은지 다시 확인한다.

### 3. Test·fixture를 명시적으로 stage

```powershell
git add -- tests/test_answer_presentation.py tests/fixtures/transfusion_multimodal_retrieval.json tests/fixtures/transfusion_retrieval_baseline.json
git add -- tests/test_bm25_e5_strategy_evaluation.py tests/test_bm25_vector_fairness.py tests/test_controlled_paraphrasing.py tests/test_evidence_routing.py tests/test_multilingual_e5_fairness.py tests/test_multimodal_web_uat.py
git add -- tests/test_retrieval_comparison_app.py tests/test_retrieval_strategy_evaluation.py tests/test_schat_multimodal_mvp.py tests/test_schat_mvp_stabilization.py tests/test_structured_table_evidence.py tests/test_table_evidence_app.py tests/test_transfusion_chroma_baseline.py
```

### 4. Evaluation tool을 명시적으로 stage

```powershell
git add -- tools/bm25_e5_strategy_evaluate.py tools/bm25_vector_fairness_evaluate.py tools/chroma_baseline_evaluate.py tools/multilingual_e5_fairness_evaluate.py tools/multimodal_retrieval_evaluate.py
git add -- tools/retrieval_baseline_metrics.py tools/retrieval_comparison_app.py tools/retrieval_strategy_evaluate.py tools/schat_multimodal_mvp_evaluate.py tools/schat_mvp_stabilization_report.py tools/schat_mvp_stabilize.py tools/structured_table_evidence.py tools/table_evidence_app.py
```

### 5. 기술 문서·현재정본을 stage

```powershell
git add -- docs/rag/39_TRANSFUSION_CHROMADB_RAGAS_BASELINE_RESULT.md docs/rag/40_RETRIEVAL_BASELINE_AND_SELECTION_RESULT.md docs/rag/42_SCHAT_MVP_STABILIZATION_RESULT.md docs/rag/44_BM25_VECTOR_FAIRNESS_VALIDATION_RESULT.md docs/rag/45_MULTILINGUAL_E5_LARGE_FAIRNESS_RESULT.md docs/rag/50_SCHAT_MULTIMODAL_MVP_RESULT.md docs/rag/50_SCHAT_RETRIEVAL_GENERATION_MULTIMODAL_RESULT.md
git add -- docs/superpowers docs/현재정본 변경이력.md 복구기준점.md
```

### 6. Raw-free artifact를 명시적으로 stage

```powershell
git add -- artifacts/2026-09-16_transfusion-chromadb-ragas-baseline/chroma_results.csv artifacts/2026-09-16_transfusion-chromadb-ragas-baseline/chroma_results.json artifacts/2026-09-16_transfusion-chromadb-ragas-baseline/chroma_summary.json artifacts/2026-09-16_transfusion-chromadb-ragas-baseline/comparison_schema.json artifacts/2026-09-16_transfusion-chromadb-ragas-baseline/dataset_manifest.json artifacts/2026-09-16_transfusion-chromadb-ragas-baseline/environment.json artifacts/2026-09-16_transfusion-chromadb-ragas-baseline/metrics_by_question_type.csv artifacts/2026-09-16_transfusion-chromadb-ragas-baseline/ragas_results.json artifacts/2026-09-16_transfusion-chromadb-ragas-baseline/review.html artifacts/2026-09-16_transfusion-chromadb-ragas-baseline/test_results.json
git add -- artifacts/2026-09-16_transfusion-retrieval-baseline
git add -- artifacts/2026-09-17_bm25-e5-retrieval-strategy-02 artifacts/2026-09-17_bm25-vector-fairness-validation artifacts/2026-09-17_multilingual-e5-large-fairness-validation
git add -- artifacts/2026-09-17_schat-multimodal-mvp artifacts/2026-09-17_schat-mvp-stabilization artifacts/2026-09-17_schat-retrieval-generation-multimodal artifacts/2026-09-17_transfusion-expanded-retrieval artifacts/프로젝트현황
```

### 7. Commit 전 검수

```powershell
git status --short
git diff --cached --name-status
git diff --cached --check
```

이때 `tmp/`, virtual environment, model cache, secret 또는 병원 원본 파일이 staged 목록에 있으면 commit하지 말고 먼저 제외한다.

### 8. 사용자 승인 후 commit·tag·push

```powershell
git commit -m "Create SCHAT v0.8 stable recovery point"
git tag -a v0.8 -m "SCHAT text and table MVP stable recovery point"
git push origin boha-rag
git push origin v0.8
```

위 명령은 사용자 승인 아래 실행한 저장 절차의 기록이다. 이후 같은 tag를 다시 만들거나 이동하지 않는다.

## 저장된 tag로 복구하는 안전한 방법

Tag가 실제로 생성된 뒤 문제가 생기면 기존 작업을 강제로 지우지 말고 새 복구 branch에서 먼저 확인한다.

```powershell
git fetch origin --tags
git switch -c recovery-v0.8 v0.8
git status
```

확인 후 기존 branch 처리 방법을 결정한다. `git reset --hard`나 force push는 이 안내의 기본 복구 방식이 아니다.

## 문서 역할

- `docs/현재정본/`: 현재 최신 상태
- `docs/superpowers/progress/`: 작업 진행 히스토리
- `docs/rag/`: RAG 상세 기술 기록
- `docs/bm25/`: BM25 상세 기술 기록
- `artifacts/`: 평가 결과와 증거
- `변경이력.md`: 사람이 보는 주요 변경 기록
- `복구기준점.md`: 이 Git 복구 안내
