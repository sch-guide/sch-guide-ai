# SCHAT 복구 기준점

## 현재 공식 안정 기준점

| 항목 | 값 |
|---|---|
| Tag | `v0.9` |
| Commit | `git rev-parse v0.9^{commit}`으로 확인 |
| Branch | `boha-rag` |
| 상태 | GitHub에 저장된 annotated recovery tag |

## 이전 안정 기준점

| 항목 | 값 |
|---|---|
| Tag | `v0.8` |
| Commit | `a1ce3207f4fd786bb6e40a179f9f6930485df6fb` |
| 상태 | 기존 commit/tag 그대로 보존 |

`v0.8`은 삭제·이동·재생성하지 않는다. 문제가 생기면 `v0.8` 또는 `v0.9` 중 필요한 기준점에서 별도 복구 branch를 만들 수 있다.

## v0.9 검증 상태

| 항목 | 값 |
|---|---|
| 상태 | 검증 및 GitHub 저장 완료 |
| Full pytest | 534 passed, 4 skipped, 7 warnings |
| Ruff | 통과 |
| `git diff --check` | 통과 |
| Security audit | exact source 0, forbidden field 0, secret marker 0 |
| Code review | actionable defect 0 |

### v0.9 포함 범위

- Gold-independent frozen reranker bottleneck evaluator와 raw-free 결과
- Sparse table parser fallback과 numeric/time table scoring
- MM003 포함 approved table UAT 5/5 Top-10
- Controlled-generation invalid/pending extractive fallback
- Image+table mixed routing metadata, image human-review gate
- 최종 현재정본, progress, 결과 문서와 raw-free artifact

### 제외·보류 범위

- `.env`, Streamlit secrets, API key
- 병원 원본 PDF
- `.venv/`, `.venv-eval-transfusion/`, `data/models/`, `tmp/`
- 재생성 가능한 Chroma/HNSW binary index
- Superseded artifact 디렉터리
- Provider Live response/prompt
- Exact SourceUnit/table source text artifact

## 안전한 복구 방법

기존 작업을 강제로 덮어쓰지 말고 별도 branch에서 확인한다.

```powershell
git fetch origin --tags
git switch -c recovery-v0.9 v0.9
git status
```

이전 안정점이 필요하면 별도 branch 이름으로 `v0.8`을 사용한다.

```powershell
git switch -c recovery-v0.8 v0.8
git status
```

`git reset --hard` 또는 force push는 기본 복구 절차가 아니다.
